const sidebarReducer = (state, action) => {
    return state
}

export default sidebarReducer

/*
dispatch(action) { // (type: 'ADD-POST')
    this._state.toolPage.dialogPage = dialogReducer(this._state.toolPage.dialogPage, action);
    this._state.toolPage.postPage = profileReducer(this._state.toolPage.postPage, action)
    this._callObserver(this._state)
}*/
